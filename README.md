# 3. Test
recommend_cosine("One Last Time", "Ariana Grande", spotify_final, feature_matrix,
                 top_n = 20, same_genre = FALSE, same_artist = FALSE)
                 
Set top_n to control the number of recommendations, and use same_genre = TRUE/FALSE and same_artist = TRUE/FALSE to apply filters.
